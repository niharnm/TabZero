{
  "name": "tabzero-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "TabZero native desktop app (Tauri shell around the TabZero dashboard).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
